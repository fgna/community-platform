# Local development overrides.
# The production CMD ("node dist/main") is intentionally left intact.
# For hot-reloading, run `pnpm dev` locally instead of docker compose.
services:
  api:
    environment:
      NODE_ENV: development
      LOG_LEVEL: debug

  web:
    environment:
      NODE_ENV: development
